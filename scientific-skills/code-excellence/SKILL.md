---
name: code-excellence
description: 한글 응답 기본 설정과 코드 우수성을 위한 통합 스킬. 새 코드 작성 시 SOLID 원칙과 실용적 아키텍처 적용, 기존 프로젝트 리팩토링 시 체계적인 구조 분석 및 개선. solid-principles 스킬과 통합하여 코드 품질 보장. 한글 응답 강제.
---

# Code Excellence - 통합 코드 우수성 스킬

**중요: 이 스킬이 활성화되면 모든 응답은 한글로 작성됩니다.**

## Overview

이 스킬은 두 가지 시나리오를 모두 지원합니다:
1. **새 코드 작성**: SOLID 원칙과 실용적 아키텍처 적용 (사전 예방적)
2. **프로젝트 리팩토링**: 전체 구조 분석 및 개선 (사후 개선적)

## Language Settings

### 🇰🇷 Korean Response Mode
**이 스킬이 활성화되면:**
- 사용자와의 모든 대화는 한글로 진행
- 오류 및 이슈 설명도 한글로
- 코드, 주석, 문서는 영어 유지 (표준 관행)

## Core Principles

### 1. File Size Limits (Token Limit: 25000)

**Claude Code Read tool은 25000 토큰 제한이 있습니다:**

**권장 파일 크기** (가독성과 유지보수성을 위해):
- **Python files**: 목표 500줄 이하, 최대 800줄 (~20000 토큰)
- **Config files**: 최대 400줄
- **Utility modules**: 목표 300줄 이하
- **Test files**: 최대 1000줄 (테스트는 더 길 수 있음)

**자동 분할 고려** - 다음의 경우 여러 파일로 분할 검토:
- 파일이 800줄을 초과할 경우
- 한 파일에 5개 이상의 클래스
- 한 파일에 20개 이상의 함수
- 3개 이상의 서로 다른 관심사가 섞여있을 경우

**코드 작성 전 자문하기:**
1. ✅ 이 파일이 읽기 쉬운 크기(500줄 이하)일까?
2. ✅ 더 작은 집중된 파일들로 분할할 수 있을까?
3. ✅ 각 파일이 명확한 단일 목적을 가지고 있을까?

### 2. Class Size Guidelines

**권장 클래스 크기** (가독성과 유지보수성을 위해):
- **일반 클래스**: 목표 225줄 이하, 최대 450줄
- **데이터 클래스**: 최대 150줄
- **복잡한 로직 클래스**: 목표 300줄 이하, 최대 600줄
- **테스트 클래스**: 최대 750줄 (테스트는 더 길 수 있음)

**분할 검토** - 다음의 경우 클래스 분할 고려:
- 클래스가 450줄을 초과할 경우
- Public 메서드가 30개 이상일 경우
- 서로 관련 없는 메서드들이 섞여있을 경우
- 여러 이유로 변경될 경우 (단일 책임 원칙 위반)

**클래스 설계 시 자문하기:**
1. ✅ 이 클래스가 읽기 쉬운 크기(300줄 이하)일까?
2. ✅ 메서드가 100줄을 넘으면, 더 작게 나눌 수 있는가?
3. ✅ 클래스가 한 가지 일만 하는가?
4. ✅ 메서드들이 서로 관련있는가?

### 3. SOLID Principles Integration

**새 코드 작성 시 SOLID 원칙을 따릅니다:**

#### SOLID 원칙 (solid-principles 스킬 참조):
- **S**ingle Responsibility: 단일 책임 원칙
- **O**pen/Closed: 개방-폐쇄 원칙
- **L**iskov Substitution: 리스코프 치환 원칙
- **I**nterface Segregation: 인터페이스 분리 원칙
- **D**ependency Inversion: 의존성 역전 원칙

#### 기본 설계 원칙:
- **단일 책임**: 각 클래스는 하나의 명확한 목적
- **실용적 디자인**: 완벽함보다 간단하고 동작하는 솔루션
- **조기 추상화 방지**: 진짜 필요할 때만 추상화
- **Protocol 우선**: 인터페이스를 먼저 정의
- **의존성 주입**: 구체 클래스가 아닌 추상화에 의존

#### 좋은 예시: SOLID 원칙 준수
```python
# ✅ GOOD: 단일 책임, 명확한 인터페이스
from typing import Protocol

class AuthService(Protocol):
    def authenticate(self, username: str, password: str) -> bool:
        ...

class UserAuthentication:
    def __init__(self, auth_service: AuthService):
        self.auth_service = auth_service  # 의존성 주입
        self.sessions = {}

    def login(self, username, password):
        if self.auth_service.authenticate(username, password):
            # 인증 로직
            pass

    def logout(self, session_id):
        pass

    def validate_session(self, session_id):
        pass
```

#### 나쁜 예시: SOLID 위반
```python
# ❌ AVOID: 여러 책임, 직접 의존성
class UserManager:
    def __init__(self):
        self.db = MySQLDatabase()  # 구체 클래스에 직접 의존

    def login(self): ...           # 인증
    def save_to_db(self): ...      # 데이터베이스
    def send_email(self): ...      # 이메일
    def generate_report(self): ... # 리포트 생성
    # 너무 많은 책임!
```

**상세한 SOLID 원칙은 solid-principles 스킬을 참조하세요**

## Two Modes of Operation

### Mode 1: 새 코드 작성 (SOLID + Practical Architecture)

**언제 사용**: 새 기능 구현, 코드 작성, 설계 시

**원칙**:
1. **SOLID 원칙 준수**
   - Protocol/Interface 먼저 정의
   - 의존성 주입 사용
   - 상속보다 컴포지션 사용
   - 단일 책임 원칙 준수

2. **크기 제한 준수**
   - 파일: 목표 500줄, 최대 800줄
   - 클래스: 목표 225줄, 최대 450줄
   - 메서드: 100줄 넘으면 분할 검토

3. **실용적 디자인**
   - 간단하고 동작하는 솔루션 선택
   - 조기 추상화 금지
   - 명확한 책임 분리

**사용 시나리오**:
- "새 기능 구현해줘"
- "사용자 인증 시스템 만들어줘"
- "데이터 처리 파이프라인 설계해줘"

**상세 가이드**: 더 자세한 내용은 [references/compact-architecture.md](references/compact-architecture.md) 참조

---

### Mode 2: 프로젝트 리팩토링 (Structure + SOLID)

**언제 사용**: 기존 프로젝트 구조 개선, 레거시 코드 정리

**5단계 워크플로우**:

#### Phase 1: 분석 (필수 첫 단계)

1. **프로젝트 구조 분석**
   - Glob과 Grep으로 현재 파일 구조 매핑
   - 모든 Python 파일의 위치와 크기 식별
   - 중복 파일 찾기 (다른 위치의 같은 이름)
   - 흩어진 테스트 파일 위치 파악
   - `__init__.py` 파일 확인
   - `requirements.txt`, `setup.py` 존재 여부 확인

2. **코드 품질 분석**
   - **파일 크기 분석**:
     - 🔴 >800줄 파일 (~20000 토큰): 분할 권장
     - 🟡 >500줄 파일 (~12500 토큰): 분할 검토
     - 🟢 <500줄 파일: 적정 크기
   - **클래스 크기 분석**:
     - 🔴 >450줄 클래스: 분할 권장
     - 🟡 >300줄 클래스: 분할 검토
     - 🟢 <225줄 클래스: 적정 크기
   - 큰 파일 (>50KB) 식별 및 분할 필요성
   - SOLID 원칙 위반 찾기 (개요만, 상세 검증은 Phase 3에서)
   - 코드 중복 패턴 탐지
   - import 문과 의존성 체인 분석

3. **개선 계획 생성**
   - 심각도별 현재 이슈 분류 (🔴 Critical, 🟡 Moderate, 🟢 Minor)
   - 제안 디렉토리 구조
   - 리팩토링 권장사항
   - 단계별 마이그레이션 계획
   - **사용자 승인을 받은 후 진행**

#### Phase 2: 구조 리팩토링 (사용자 승인 후)

1. **대상 디렉토리 구조 생성**
   ```
   src/
   ├── __init__.py
   ├── pipelines/
   │   └── __init__.py
   ├── analyzers/
   │   └── __init__.py
   ├── visualizers/
   │   └── __init__.py
   ├── reporters/
   │   └── __init__.py
   └── utils/
       └── __init__.py

   tests/
   ├── __init__.py
   ├── unit/
   │   └── __init__.py
   └── integration/
       └── __init__.py

   cli.py
   requirements.txt
   setup.py
   README.md
   ```

2. **파일 이동 및 정리**
   - 적절한 디렉토리로 파일 이동
   - 모든 필요한 `__init__.py` 파일 생성
   - 코드베이스 전체에서 import 경로 업데이트
   - 중복 파일 통합

3. **파일 크기 최적화**
   - 800줄 초과 파일을 집중된 모듈로 분할 (각각 <500줄 목표)
   - 450줄 초과 클래스를 여러 파일로 분할 (각 클래스 225줄 이하 목표)
   - 섞인 관심사를 별도 파일로 추출

4. **패키지 파일 생성**
   - import에서 `requirements.txt` 생성
   - 적절한 패키지 설정으로 `setup.py` 생성
   - 새 구조로 `README.md` 생성/업데이트

#### Phase 3: SOLID 원칙 검증 및 적용

**이 단계에서는 solid-principles 스킬을 호출합니다:**

사용자에게 알림:
> "이제 **solid-principles 스킬**을 사용하여 코드 품질을 검증하고 개선하겠습니다."

**solid-principles 스킬이 수행할 작업:**
- SOLID 위반 자동 감지 및 분석
- 리팩토링 패턴 제안
- Protocol/Interface 우선 설계 적용
- 의존성 주입 패턴 구현
- 계층화된 프로젝트 구조 제안

**검증 항목:**
- 단일 책임 원칙 위반 여부
- 개방-폐쇄 원칙 준수 여부
- 리스코프 치환 원칙 위반 여부
- 인터페이스 분리 원칙 적용 여부
- 의존성 역전 원칙 준수 여부

#### Phase 4: 코드 중복 제거

1. **중복 코드 식별**
   - 파일 간 반복 로직 찾기
   - 유사한 analyzer/visualizer 구현 탐지

2. **공통 패턴 추출**
   - 공통 기능을 위한 기본 클래스 생성
   - 재사용 가능한 유틸리티 함수 구축
   - 적절한 경우 템플릿 메서드 패턴 구현

#### Phase 5: 통합 & 테스트

1. **CLI 진입점 생성**
   - 모든 기능을 위한 통합 `cli.py` 구축
   - 인자 파싱 구현
   - 도움말 문서 추가

2. **Import 검증**
   - 모든 import 경로가 올바르게 작동하는지 테스트
   - 순환 의존성이 없는지 확인
   - 패키지 설치 검증

3. **문서 생성**
   - docstring 업데이트
   - 아키텍처 문서 생성
   - 사용된 패턴 문서화

**사용 시나리오**:
- "이 프로젝트 리팩토링해줘"
- "레거시 코드 정리해줘"
- "프로젝트 구조 개선해줘"

**상세 가이드**: 더 자세한 워크플로우는 [references/refactoring-workflow.md](references/refactoring-workflow.md) 참조

## Key Red Flags to Fix

### 파일/클래스 크기 위반:
- 🔴 800줄 초과 파일 - 분할 권장
- 🟡 500줄 초과 파일 - 분할 검토
- 🔴 450줄 초과 클래스 - 분할 권장
- 🟡 300줄 초과 클래스 - 분할 검토
- 🔴 100줄 초과 메서드 - 분할 가능성 검토
- 🔴 3개 이상의 서로 다른 관심사가 섞인 파일 - 분할

### SOLID 위반 신호 (solid-principles 스킬에서 상세 확인):
- ❌ "Manager", "Handler", "Utility"라는 이름의 클래스
- ❌ 이름에 "and"가 들어간 메서드
- ❌ 모든 곳에서 구체 클래스를 직접 인스턴스화
- ❌ 타입별 동작을 위한 `isinstance()` 체크
- ❌ 타입 선택을 위한 큰 if/elif 체인
- ❌ 30개 이상의 public 메서드를 가진 God 클래스

## File Splitting Strategy

### 파일 타입별 분할 전략:

```python
# Pipeline 파일: 처리 단계별 분할
src/pipeline/
├── loader.py          (200-400줄)
├── preprocessor.py    (300-500줄)
├── analyzer.py        (400-600줄)
└── exporter.py        (200-400줄)

# Analysis 파일: 분석 타입별 분할
src/analysis/
├── base.py            (150-200줄 - 기본 클래스)
├── structure.py       (400-600줄)
├── noise.py           (400-600줄)
└── comparison.py      (300-500줄)

# Visualization 파일: 플롯 타입별 분할
src/visualization/
├── base_plotter.py    (200-300줄)
├── heatmaps.py        (400-600줄)
├── scatter_plots.py   (300-500줄)
└── styles.py          (100-200줄)
```

## Design Patterns

### Protocol Pattern (인터페이스 정의)
```python
from typing import Protocol, runtime_checkable

@runtime_checkable
class Analyzer(Protocol):
    def analyze(self, data: pd.DataFrame) -> dict:
        ...

    def get_results(self) -> dict:
        ...
```

### Factory Pattern (객체 생성)
```python
class AnalyzerFactory:
    _analyzers = {}

    @classmethod
    def register(cls, name: str):
        def decorator(analyzer_class):
            cls._analyzers[name] = analyzer_class
            return analyzer_class
        return decorator

    @classmethod
    def create(cls, name: str, **kwargs):
        analyzer_class = cls._analyzers.get(name)
        if not analyzer_class:
            raise ValueError(f"Unknown analyzer: {name}")
        return analyzer_class(**kwargs)
```

### Import Organization
```python
# Standard library
import os
import sys

# Third-party
import numpy as np
import pandas as pd

# Local
from src.analyzers.base import BaseAnalyzer
from src.utils.helpers import load_data
```

## Integration with Other Skills

### solid-principles 스킬과의 통합

이 스킬은 **solid-principles 스킬과 함께 작동**합니다:
- **code-excellence**: 프로젝트 구조, 파일 조직, 전체 아키텍처 담당
- **solid-principles**: SOLID 원칙 상세 검증 및 적용 담당

**사용 패턴**:
```
1. code-excellence 스킬 활성화 → 프로젝트 구조 분석 (Phase 1-2)
2. Phase 3에서 solid-principles 스킬 호출 → SOLID 원칙 검증 및 적용
3. code-excellence 스킬로 복귀 → 중복 제거 및 통합 (Phase 4-5)
```

## Resources

### references/

- **[compact-architecture.md](references/compact-architecture.md)**: 실용적 아키텍처의 상세 원칙과 예시
- **[refactoring-workflow.md](references/refactoring-workflow.md)**: 5단계 리팩토링 워크플로우의 상세 가이드

**언제 읽기**:
- 새 코드 작성 시 `compact-architecture.md` 읽기
- 프로젝트 리팩토링 시 `refactoring-workflow.md` 읽기

### scripts/

- **project_analyzer.py**: 프로젝트 분석 구현을 위한 템플릿

## Communication Style

- 심각도 지표를 사용해 분석 결과를 명확하게 제시
- 변경 전/후 구조 비교 표시
- 특정 코드에 각 원칙이 왜 중요한지 설명
- 가독성을 위해 시각적 구분자 사용
- 주요 리팩토링 전에 항상 사용자 승인 받기
- solid-principles 스킬 호출 시 명확히 알림

## Important Notes

- Phase 1 분석을 절대 건너뛰지 마세요
- 변경 실행 전에 항상 계획을 제시하세요
- 리팩토링 중 기존 기능을 그대로 유지하세요
- 모든 구조적 변경 후 import를 테스트하세요
- 가능한 경우 git 히스토리를 보존하세요 (git mv 사용)
- 영향받는 모든 import 문을 즉시 업데이트하세요
- 대규모 리팩토링 작업의 백업을 생성하세요
- **Phase 3에서는 반드시 solid-principles 스킬을 호출하세요**

## Success Criteria

✅ 모든 파일이 논리적 디렉토리 구조로 정리됨
✅ 모든 패키지에 `__init__.py` 파일 있음
✅ 중복 코드나 파일 없음
✅ **모든 파일 <800줄, 클래스 <450줄 (가독성 보장)**
✅ **읽을 때 25000 토큰 제한을 초과하는 파일 없음**
✅ 전체에 걸쳐 SOLID 원칙 준수 (solid-principles 스킬로 검증됨)
✅ 적절한 의존성 관리 (`requirements.txt`, `setup.py`)
✅ 명확한 진입점 (`cli.py`)
✅ 모든 import가 올바르게 작동함
✅ 코드가 더 유지보수 가능하고 확장 가능함
✅ **각 파일과 클래스가 단일하고 명확한 책임을 가짐**

## 사용 예시

### 새 코드 작성:
```
사용자: "사용자 인증 시스템 만들어줘"
→ Mode 1 활성화
→ SOLID 원칙 준수하여 설계
→ Protocol 먼저 정의, 의존성 주입 사용
→ 클래스는 225줄 이하 목표로 유지
```

### 프로젝트 리팩토링:
```
사용자: "이 프로젝트 리팩토링해줘"
→ Mode 2 활성화
→ Phase 1: 전체 구조 분석 (파일/클래스 크기, 중복)
→ Phase 2: 디렉토리 재구성, 800줄 초과 파일 분할
→ Phase 3: solid-principles 스킬 호출하여 SOLID 원칙 검증
→ Phase 4: 중복 제거
→ Phase 5: 통합 & 테스트
```
